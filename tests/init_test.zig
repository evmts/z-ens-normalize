const std = @import("std");
const testing = std.testing;

test "init functions compile" {
    // This test just verifies that init.zig compiles successfully
    // Actual testing of the functions happens within the module
    try testing.expect(true);
}
